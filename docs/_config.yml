# ── GitHub Pages / Jekyll config ─────────────────────────────
title: "Haedyn Darling-Hill — Engineering Portfolio"
description: "Engineering portfolio — CAD, built projects, and code."
url: "https://haedyndh.github.io"
baseurl: "/haedyndarlinghillportfolio"

# Exclude dev files from the build
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# This site is pure static HTML — no theme needed
theme: null
plugins: []